local:
    root: /var/www/presales-blt/docroot
    uri: 'http://lc.presales-blt'
    host: lc.presales-blt
    user: vagrant
    ssh: { options: '-o PasswordAuthentication=no -i $HOME/.vagrant.d/insecure_private_key' }
